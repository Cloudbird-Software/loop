# 本 workflow 已迁移到 product-x 仓库（D0 决策，见 DECISIONS.md ADR-002）。
# 现位置：Cloudbird-Software/product-x/.github/workflows/materializer.yml
# 触发：push to product-x main, paths waves/**
# token：product-x workflow 默认 GITHUB_TOKEN（免 conductor app secret）
# 本文件保留为转发锚点，不再执行任何 job。
name: materializer (migrated to product-x)
on:
  workflow_dispatch:
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "materializer has migrated to product-x repo (see DECISIONS.md ADR-002)"
