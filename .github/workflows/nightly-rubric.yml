name: nightly-rubric
on:
  schedule: [{cron: "0 2 * * *"}]
  workflow_dispatch:
permissions: {contents: read, issues: write}
jobs:
  rubric:
    runs-on: ubuntu-latest
    environment: eval
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
      - name: Run promptfoo llm-rubric
        env:
          LLM_GATEWAY_KEY: ${{ secrets.LLM_GATEWAY_KEY }}
        run: |
          set -e
          # TODO: promptfoo eval -c promptfoo/promptfooconfig.yaml --filter-metadata rubric=true
          # 回归只开 Finding，不阻塞
          echo "nightly-rubric: TODO — promptfoo config not yet scaffolded (W2)"
