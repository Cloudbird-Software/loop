# V-013 — 两沙盒并发 CAS+租约+reaper 不出错

```yaml
id: V-013
title: 两沙盒同时领不同卡，CAS 不冲突、路径租约互斥、reaper 不误杀
type: Verify
role: verify
tier: standard
charter: ["G0"]
paths:
  - loopd/loopd.py
  - bootstrap.sh
acceptance:
  - 两沙盒同时 loop next，领到不同卡（journal 无重复领卡）
  - 两沙盒 paths 交叉时，后到的被拒（loopd 拒绝日志）
  - 一沙盒假死超租约，reaper 回收其卡，另一沙盒可领
  - 全程无死锁、无重复 done
verify_target: C-013
depends_on: [C-013]
ready: false
status: pending
```

## 前置依赖检查

- [C-013](./C-013.md) 必须先 done（impl 侧已跑过并发场景）。
- 实际上本卡 = C-013 的 verify 侧独立复核。
- 若 C-013 未 done → **不要开始**。

## 验证步骤（独立 verify 视角复核 C-013 的并发场景）

1. 找 C-013 跑过的并发场景 journal。
2. 不读 C-013 impl AI 的过程评论（盲一半）。
3. 独立复核三个场景：
   - **CAS 不冲突**：两沙盒同时 loop next，查 journal 是否有重复领卡记录（应为无）？
   - **路径租约互斥**：两沙盒 paths 交叉，后到的被拒（查 loopd 拒绝日志）？
   - **reaper 不误杀**：假死沙盒超租约，reaper 回收其卡（查 reaper 日志），另一沙盒可领该卡（查领卡记录）？
4. 客观记录每个场景的证据（journal 行号、日志原文）。
5. 逐条对 acceptance 记 PASS/FAIL。
6. 全 PASS → VERDICT=PASS；任一 FAIL → 建 F-0NN。

## 禁止

- 不要读 C-013 的过程评论（盲一半）。
- 不要在测试中误杀真实在跑的卡（用专用测试卡）。
- 不要在 reaper 假死测试中污染生产 journal。

## 评论

_（验证时在此评论 VERDICT + 证据）_
