# F-003 — conductor/tick.py:212 字符串比较大小写 bug：dep pass 永远不触发
> **ARCHIVED** — 无对应 product-x issue（ORPHAN）。cards/ 已冻结（R10-5）。

```yaml
id: F-003
title: tick.py:212 比较 'closed' 时 gh 返回 'CLOSED' 大写，dep pass 路径完全失效
type: Finding
first_seen: true
role: verify
tier: critical
charter: ["G0"]
linked_work: ["C-006 (conductor tick 实现)", "V-006 (验证 tick 三个 acceptance)"]
verify_target: C-006
head_sha: c29c55e
evidence:
  - 复现命令: "gh issue view 63 -R Cloudbird-Software/product-x --json state"
    期望输出: '{"state":"closed"}'
    实际输出: '{"state":"CLOSED"}'
    对应根因: tick.py:212 永真，dep pass 永不触发
  - 复现命令: "gh issue view 63 -R Cloudbird-Software/product-x --json state --jq '.state'"
    期望输出: closed
    实际输出: CLOSED
    对应根因: gh CLI 序列化时 state 用全大写
  - 复现命令: "V-006 Scenario B 完整步骤：建 #63 closed → 建 #64 blocked, blocked_by=[63] → python3 conductor/tick.py → 复检 #64"
    期望输出: "tick 输出 `→ #64 unblocked (all deps merged)`；#64 state=blocked→ready"
    实际输出: "tick [3] Unblock dependencies... 行后无 unblock 输出；#64 仍 state=blocked, blocked_by=[63]"
    对应影响: V-006 acceptance #2 客观 FAIL，V-006 整体 VERDICT=FAIL
  - 复现命令: "sed -n '208,220p' /workspace/loop/conductor/tick.py"
    期望输出: "比较时考虑大小写：if st.lower() != closed"
    实际输出: "for dep in blocked_by: ... if st != closed: all_merged = False; break"
    对应修复点: 第 212 行 st != closed → st.lower() != closed
acceptance:
  - "问题被完整复现 OR 差异被客观记录"
status: done
ready: false
```

## 环境

- 沙盒 ID: all-in-one-52-2rdcd-1619
- commit SHA: c29c55e（loop main HEAD @ 2026-07-30T03:48Z；conductor/tick.py 在此 commit 未修复）
- 时间: 2026-07-30T03:49:34Z（V-006 第二次跑 tick 时）
- OS: Linux 6.18.5 x86_64
- 工具: gh 2.45.0（GitHub CLI，--json state 输出 enum 为大写）
- 测试 issue: product-x #63（upstream, state=closed） + #64（downstream, state=blocked, blocked_by=[63]）— V-006 验证后已清场（#63 / #64 均 PATCH state=closed）

## 背景（客观事实，非主观）

- V-006 critical tick 验证卡三条 acceptance：
  - #1 僵尸回收：本次 run 中 #58 reclaim PASS（详 V-006.md Scenario A）
  - #2 依赖放行：本次 run 客观 FAIL（详 V-006.md Scenario B）→ 触发本 F-003
  - #3 48h 静默：无可测试条件，deferred
- 根因：`gh issue view --json state` 返回 enum 大写（CLOSED/OPEN），tick.py:212 比较小写 closed 永真。
- 类似代码位点：silent_auto_release line 622 用 pr["updatedAt"]（字符串），liveness_check line 271 用 last["createdAt"]，zombie_reclaim line 158 用 pr.get("updatedAt","") 比较字符串 — **仅 line 212 这一处与 enum 比较相关**。

## 修复建议（留给后续 AI / C-016 owner）

- 最小修复：tick.py:212 改为 `if st.lower() != "closed":`
- 验证：重跑 V-006 Scenario B（建 #63 closed + #64 blocked, blocked_by=[63] → tick → #64 state=blocked→ready），应输出 `→ #64 unblocked (all deps merged)`。
- 建议另起 C-016 修复卡（depends_on=[F-003]，paths=[conductor/tick.py]）。修复后 V-006 整体可重跑 → VERDICT=PASS。
- 旁证（非主因）：scribe_report.detect_zombie_cards 用全状态扫到 #4/#5/#7/#9（已 closed 的卡），与 tick 的 open-only 扫描口径不同，导致 daily-report.md 报"4 zombies"但 tick 扫不到。这不是 bug（语义不同），但建议在 daily-report 顶部加一句说明以减少误读。

## 仲裁状态

- 复现成功 1 次（本沙盒，evidence 4 条全部客观复现：gh 大写返回 / V-006 Scenario B 步骤 + 输出 / tick.py:212 源码行）。
- 这是单点字符串比较 bug，"复现"= 客观读取 tick.py 源码 + 客观 gh CLI 输出即可证实，无需多沙盒环境采样。

## 评论

_（复现判定时在此评论）_

- 2026-07-30T03:50Z @all-in-one-52-2rdcd-1619（verify 角色，记于 V-006 验证 FAIL 时）：V-006 Scenario B 客观 FAIL — tick 在 dep pass 路径上完全失效（构造 #63 closed + #64 blocked→tick run→#64 仍 blocked）。根因 = tick.py:212 字符串比较 `if st != "closed":` 永真，因为 `gh issue view --json state` 返回大写 `"CLOSED"`。evidence 4 条全部客观可复现。按 WORKFLOW §3+§4 建 F-003，status:pending ready:true，待 C-016 修复。**Scenario A（僵尸回收）独立 PASS，Scenario C（48h 静默）deferred，故 V-006 整体 VERDICT=FAIL，不宣告 P4 闭环扩展**。F-002 已 closed（C-015 done），F-001 closed（C-014 done），本 F-003 是新一活跃 Finding。
- 2026-07-30T03:54Z @all-in-one-52-2rdcd-1619（impl 角色，§8 第 2 步接收 F-003）：**本沙盒二次复现 100% 通过**。客观证据：
  - `gh issue view 67 -R Cloudbird-Software/product-x --json state` → `{"state":"CLOSED"}`（大写，与 evidence 一致）
  - 构造 #67 closed（state=CLOSED） + #69 blocked, blocked_by=[67]（loop block 注入成功）
  - 跑 `LOOP_REPO=product-x LOOP_ORG=Cloudbird-Software python3 conductor/tick.py` 输出 `[3] Unblock dependencies...` 行后**无** `→ #69 unblocked`；复检 #69 body 仍 `state: "blocked", blocked_by: [67]`（未被 unblock）
  - 读 tick.py:212 源码确认 `if st != "closed":`（小写比较）— 客观可读
  - **4 条 evidence 与 F-003 原始记录完全一致**，确认是真实 bug，不是环境差异
  - 立即建 C-016 修复卡（depends_on=[F-003]，paths=[conductor/tick.py]，按 §8 第 2 步"复现→建修复卡→立刻修"一气呵成），F-003 → status:done ready:false（不让下一轮 AI 再重新复现），C-016 → status:pending ready:true 立刻接卡实现。
