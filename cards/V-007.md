# V-007 — verify.required=true 卡调 verify.sh 非退化态

```yaml
id: V-007
title: verify.required=true 的卡跑 verify，确认 .loop/verify.sh 被调用非退化态
type: Verify
role: verify
tier: standard
charter: ["G0"]
paths:
  - product-x/.loop/verify.sh
  - loopd/loopd.py
acceptance:
  - 一张 verify.required=true 的卡跑 loop verify
  - 输出不再是 "PASS (no verify.sh declared)" 退化态
  - .loop/verify.sh 被调用，退出码 0=PASS/1=FAIL 生效
  - FAIL 时 stderr 输出失败断言（供 verify AI 抓证据）
verify_target: C-007
depends_on: [C-007]
ready: false
status: pending
```

## 前置依赖检查

- [C-007](./C-007.md) 必须先 done（contracts + verify.sh 存在）。
- 若 C-007 未 done → **不要开始**。

## 验证步骤

1. 确认 [C-007](./C-007.md) done，.loop/verify.sh 存在且可执行。
2. 找一张 verify.required=true 的卡（或临时造一张测试卡）。
3. 跑 `loop verify`（或对应路径）。
4. 客观记录：
   - 输出是否含 verify.sh 的实际执行结果（非退化态）？
   - 退出码是否正确（0/1）？
   - FAIL 时 stderr 是否输出失败断言？
5. 逐条对 acceptance 记 PASS/FAIL。
6. 全 PASS → VERDICT=PASS；任一 FAIL → 建 F-0NN。

## 禁止

- 不要手工跑 verify.sh 绕过 loop verify（要验 loopd 调用链）。
- 不要在 verify.sh 退化态时强行 PASS。

## 评论

_（验证时在此评论 VERDICT + 证据）_
