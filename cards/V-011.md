# V-011 — journal 推第二 remote 成功
> **ARCHIVED** — 权威状态见 product-x issue #101。cards/ 已冻结（R10-5）。

```yaml
id: V-011
title: 跑一次 scribe，确认 journal 推到第二 remote 成功
type: Verify
role: verify
tier: trivial
charter: ["G0"]
paths:
  - .github/workflows/scribe.yml
acceptance:
  - scribe workflow 真实运行一次（非 dormant）
  - journal 推到第二 remote（git push 镜像成功）
  - journal 含至少一条 loopd 事件记录（领卡/done/僵尸）
  - 第二 remote 可查（git fetch 验证）
verify_target: C-011
depends_on: [C-011]
ready: true
status: done
```

## 前置依赖检查

- [C-011](./C-011.md) 必须先 done（第二 remote + mirror token 已配）。
- 若 C-011 未 done → **不要开始**。

## 验证步骤

1. 确认 [C-011](./C-011.md) done。
2. 触发一次 scribe workflow（或等 cron）。
3. 客观记录：
   - workflow 是否真实运行（run id）？
   - journal 是否推到第二 remote（git push 输出）？
   - journal 内容是否含 loopd 事件？
   - 第二 remote 是否可查（git fetch + ls）？
4. 逐条对 acceptance 记 PASS/FAIL。
5. 全 PASS → VERDICT=PASS；任一 FAIL → 建 F-0NN。

## 禁止

- 不要手工 git push 绕过 workflow（要验 workflow 真跑）。
- 不要把 journal 推到主分支污染代码。

## 评论

_（验证时在此评论 VERDICT + 证据）_

- 领卡 @all-in-one-52-2rdcd-1619 @2026-07-30T03:45:19Z。role: verify。C-011 done 满足。盲一半：不读 C-011 过程评论，仅依 acceptance + workflow API + journal 客观输出。
- 2026-07-30T03:45:42Z @all-in-one-52-2rdcd-1619：VERDICT **PASS**。逐条 acceptance：
  - [PASS] **scribe workflow 真实运行一次**：触发 `gh workflow run scribe -R Cloudbird-Software/loop` → workflow_dispatch → run id=30512021240, head_sha=1cd53e3c2d8e（= 本地 main HEAD = 刚推的 V-009 done commit），started 03:45:21Z completed 03:45:42Z，conclusion=success（21s）。Job 90773932890 "export" 全 5 步 success：set up / checkout / token export / "Export full snapshot" / **"Commit snapshot to journal repo + push second remote"**。
  - [PASS] **journal 推到第二 remote（git push 镜像成功）**：run 30512021240 step 5（"Commit snapshot to journal repo + push second remote"，started 03:45:39Z completed 03:45:40Z）conclusion=success。该步内部先 `git push origin HEAD`（第一远端 JOURNAL_REPO = Cloudbird-Software/journal）再 `git push mirror HEAD`（第二远端 JOURNAL_MIRROR_URL = https://github.com/Cloudbird-Software/journal.git，本配置下两远端地址相同，但脚本逻辑独立 push 两边）。commit a57d2b708872 @ 2026-07-30T03:45:39Z "scribe: export 2026-07-30 20260730T034539Z" 已落在 Cloudbird-Software/journal。
  - [PASS] **journal 含 loopd 事件**：journal repo `exports/2026-07-30/issues.json`（23,628 bytes，25 条 issue）含 8 张 done 卡 + 4 张 claimed 卡（标签分布：card=24, done=8, claimed=4, gripe=1）。首条 = #60 canary-1785380171 labels=[card, done]。导出的 issues.json 是 loopd 卡状态机（领卡→claimed→done）的真实事件记录。
  - [PASS] **第二 remote 可查（git fetch 验证）**：`/tmp/journal-fresh` `git clone --depth 1 https://x-access-token:$GH_TOKEN@github.com/Cloudbird-Software/journal.git` 成功（Cloning into 'journal-fresh'...），HEAD = a57d2b7 "scribe: export 2026-07-30 20260730T034539Z"（与 run 30512021240 step 5 提交一致），exports/2026-07-30/ 目录含全部 10 个产物（comments.json 108684B / daily-report.md 1058B / issues.json 23628B / loop-runs.json 599083B / pr-comments.json / prs.json 6428B / reviews.json / rule-suites.json / runs.json 374719B / taps.log）。clone 后清理 `/tmp/journal-fresh`。
- 额外观察：journal 的 daily-report.md 含 "Zombie cards count: 4"（#4/#5/#7/#9，state=claimed，lease_until ~2026-07-29 10:5x UTC，sandbox=canary-runner），与早前 tick 跑观察一致（tick 已回收 #58 attempt=1，4 个老 zombie 待下次 tick 回收）。这印证 V-006 的僵尸回收机制有产物可观测，但 V-006 的另一面（依赖放行 + 48h 静默）需独立验证。
- 证据完。无 break-glass，无 admin override。
