# C-005 — 塞测试 wave WAVE-2 进 product-x
> **ARCHIVED** — 权威状态见 product-x issue #79。cards/ 已冻结（R10-5）。

```yaml
id: C-005
title: 塞测试 wave WAVE-2 进 product-x（5 张 standard 卡，覆盖 impl/verify 全链路）
type: Card
role: planner
tier: standard
charter: ["G0"]
paths:
  - product-x/waves/WAVE-2.json
  - cards/
acceptance:
  - product-x/waves/WAVE-2.json 存在，含 5 张卡 JSON
  - 每张卡过 materializer 四校验（charter/paths 非交叉/tier/acceptance≥1）
  - 5 张卡含至少 1 张 verify.required=true（供 [C-008](./C-008.md) 闭环用）
  - depends_on 链清晰（至少 1 张卡依赖另 1 张）
depends_on: [C-003]
ready: true
status: done
```

## 前置依赖检查（领卡前必读）

- [C-003](./C-003.md) 必须先 done（planner 提示词补齐，才知道 Wave 格式）。
- 若 C-003 未 done → **不要开始**，在卡底评论"等 C-003"，去找 C-003 或其他 ready 卡。

## 背景

WAVE-1 的 5 张卡已做完（见 [agent输出2.txt](../agent输出2.txt)）。product-x 上现在**没有 ready 卡**，"继续"了也没活干。本卡塞一个测试 wave，让链路有活可干，是 [C-008](./C-008.md)（全闭环）的前置。

## 要做什么

1. 等 [C-003](./C-003.md) done，按 P3 提示词的 Wave 格式。
2. 在 product-x 建 `waves/WAVE-2.json`，含 5 张卡：
   - 卡 1（trivial）：改一个 README 拼写（impl 热身）。
   - 卡 2（standard）：加一个 API 端点（impl 主体）。
   - 卡 3（standard，verify.required=true）：为卡 2 加 unit test + acceptance 断言（impl）。
   - 卡 4（standard，depends_on 卡 3）：跑 verify 验卡 2+3（verify 角色）。
   - 卡 5（trivial，depends_on 卡 4）：清理临时文件（impl）。
3. 每张卡过 [materialize.py](../conductor/materialize.py) 四校验。
4. 至少 1 张 verify.required=true（卡 3 或卡 4），供 [C-008](./C-008.md) 跑闭环。

## 不要做什么

- 不要造 acceptance<1 的卡（materializer 拒物化）。
- 不要让 5 张卡的 paths 两两交叉（materializer 拒物化）。
- 不要塞 critical 卡（测试 wave 用 standard/trivial 即可，避免误触 critical 门禁）。

## 验收

详见 [V-005](./V-005.md)：materializer 物化 WAVE-2，过四校验，产出 milestone + parent + 5 个 Card。

## 评论

_（领卡/完成时在此评论）_
