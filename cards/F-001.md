# F-001 — W2-002 /health 端点依赖未声明的 Flask（verify FAIL）

```yaml
id: F-001
title: W2-002 /health 端点依赖未声明的 Flask，干净环境复现失败
type: Finding
first_seen: true
role: verify
tier: critical
charter: ["G0"]
linked_work: ["W2-002 (product-x/waves/WAVE-2.json)", "C-008", "V-008"]
verify_target: C-008
head_sha: 51bd2b546c3a8f4f3db38805c6826f8d0347998b   # 验证时 commit = 已合并 commit（squash），无漂移
evidence:
  - 复现命令: "cd product-x && git checkout --detach 51bd2b546c3a8f4f3db38805c6826f8d0347998b && python3 -m pytest tests/test_health.py -v"
    期望输出: "tests collected; test_health_returns_200_and_ok PASSED"
    实际输出: "ImportError during collection: ModuleNotFoundError: No module named 'flask'（tests/test_health.py:5 -> src/api/health.py:1）；1 error, 0 tests run"
    对应 acceptance: "单元测试 test_health.py 通过" → FAIL
  - 复现命令: "cd product-x && git checkout --detach 51bd2b5 && python3 src/api/health.py"
    期望输出: "Flask dev server 启动；GET /health 返回 200 + {\"status\":\"ok\"}"
    实际输出: "Traceback ... line 1, from flask import Flask, jsonify → ModuleNotFoundError: No module named 'flask'；服务未启动"
    对应 acceptance: "GET /health 返回 200 + JSON {status: ok}" → FAIL（端点无法启动）
  - 复现命令: "cd product-x && ls requirements.txt pyproject.toml setup.py setup.cfg Pipfile 2>/dev/null; grep -ri flask --include='*.txt' --include='*.toml' --include='*.cfg' --include='*.in' ."
    期望输出: "若 impl 用 Flask，则 Flask 应在某依赖清单中声明（P0.md：新增外部依赖前必须 loop upstream）"
    实际输出: "product-x 无 requirements.txt/pyproject.toml/setup.py/setup.cfg/Pipfile；flask 不出现在任何 manifest。loop 仓 requirements.txt 仅含 PyYAML。即 impl 沙盒恰巧预装了 Flask，代码不可移植、不可复现。"
    根因: 未声明的外部依赖
  - 复现命令: "cd product-x && git checkout --detach 51bd2b5 && bash .loop/verify.sh"
    期望输出: "若 verify.sh 非退化态，应实际调用 /health 或 import health.py 并断言"
    实际输出: "verify.sh: 3 passed, 0 failed (exit 0)——但 3 条断言为：AGENTS.md 非空、所有 .py 仅 compile 通过（不 import）、contracts/ 含 ≥1 个 .md。未 import health.py 也未请求 /health，故未捕获缺 Flask。（C-007 验收曾要求非退化态，此处为旁证，非本 Finding 主因）"
acceptance:
  - "问题被完整复现 OR 差异被客观记录"
status: done
ready: true
```

## 环境

- 沙盒 ID: all-in-one-52-2rdcd-1619
- commit SHA: 51bd2b546c3a8f4f3db38805c6826f8d0347998b（= PR #50 squash 合并到 main 的 commit，验证时 = 合并时，无漂移）
- 时间: 2026-07-30T02:13:05Z
- OS: Linux 6.18.5 x86_64
- Python: 3.14.4, pytest 9.0.3, pluggy 1.6.0
- Flask: 未安装（`python3 -c "import flask"` → ModuleNotFoundError）

## 背景（客观事实，非主观）

- W2-002（verify.required=true）由另一沙盒 impl，PR #50 "W2-002: add /health endpoint + unit test" 于 2026-07-30T02:06:58Z 开，02:09:38Z squash 合并到 main（merge_commit 51bd2b5，head c02d8f8）。
- PR #50 有 **0 条 issue 评论、0 条 review**——即合并发生在 **verify/VERDICT 记录之前**（闭环时序倒置：impl→merge 先于 verify）。本 verify 为事后对已合并 commit 51bd2b5 的独立复核。
- impl 用 `from flask import Flask, jsonify`，但 product-x 无任何依赖清单声明 Flask。

## 仲裁状态

- 复现成功 1 次（本沙盒，完整复现全部 evidence）。
- 待后续 AI 复现：若 ≥1 复现成功 + 0 失败 → 起修复卡 C-0NN（建议：/health 改用 Python stdlib http.server，零外部依赖；或正式 loop upstream flask 并写入清单）。

## 评论

_（复现判定时在此评论）_

- 2026-07-30T02:13:05Z @all-in-one-52-2rdcd-1619：V-008 verify on 51bd2b5 → VERDICT=FAIL（acceptance 两条全 FAIL：test_health.py 收集期 ImportError；/health 端点无法启动）。根因 = 未声明的外部依赖 Flask。evidence 已客观可复现。按 WORKFLOW §4/§7 建 F-001，**不改原 W2-002/C-008 状态**（C-008 保持 in_progress）。PR #50 已合并（合并先于 verify，时序倒置），不可逆；修复走新卡 C-014（依赖含本 F-001）。
- 2026-07-30T02:14Z @all-in-one-52-2rdcd-1619（切 F-0NN 处理角色）：按 evidence 逐条在自己沙盒复现——①`pytest tests/test_health.py` → ImportError: No module named 'flask'（重现）；②`python3 src/api/health.py` → ModuleNotFoundError: No module named 'flask'（重现）；③`grep -ri flask` 任何 manifest → 无（重现）；④`bash .loop/verify.sh` → exit 0 退化态（重现）。**4/4 evidence 全部重现，复现成功**。按 WORKFLOW §8.2：改 F-001 status:done，起修复卡 [C-014](./C-014.md)（/health 改 stdlib，depends_on=[F-001]）。
