# C-008 — 造 verify.required=true 卡跑通 impl→verify→merge 全闭环 ★ P4 终点

```yaml
id: C-008
title: 造 verify.required=true 卡跑通 impl→verify→VERDICT→merge 全闭环
type: Card
role: impl
tier: critical
charter: ["G0"]
paths:
  - product-x/
  - prompts/P4.md
  - loopd/loopd.py
  - .github/workflows/
acceptance:
  - 从 WAVE-2 抽 1 张 verify.required=true 卡，跑完整闭环
  - impl 完成 → verify 跑 V-008 → VERDICT=PASS → PR 合并 → branch 推进
  - VERDICT 绑 head_sha（验证时的 commit 与合并的 commit 一致）
  - 闭环全程无人工干预（仅 AI 角色切换）
  - 闭环耗时记录（供 CHARTER G0/Q0.1 量化）
depends_on: [C-004, C-005, C-007]
ready: true
status: in_progress
```

## 前置依赖检查（领卡前必读，本卡是 P4 终点，依赖最多）

- [C-004](./C-004.md) 必须先 done（verify 提示词，否则 verify 角色跑不起来）。
- [C-005](./C-005.md) 必须先 done（WAVE-2 有 verify.required=true 的卡可验）。
- [C-007](./C-007.md) 必须先 done（contracts + verify.sh，否则 verify 退化态）。
- 任一未 done → **不要开始**，在卡底评论"等 C-0XX"，去找前置卡。

## 背景

本卡是 P4 终点 ★。跑通后 [WORKFLOW.md](./WORKFLOW.md) 暂行流程停用，切回 loopd 正式体系。当前 impl 实测过（W1 五张卡 done），但 **impl→verify→merge 这半截从没跑通**（[agent输出2.txt](../agent输出2.txt) 只有 impl 侧）。

## 要做什么

1. 等 C-004、C-005、C-007 全 done。
2. 从 WAVE-2 抽那张 verify.required=true 的卡（impl 侧完成它，产出 commit + PR）。
3. 切 verify 角色，加载 [P4.md](../prompts/P4.md)，跑 V-008：
   - 逐条跑 acceptance + .loop/verify.sh。
   - 记 VERDICT 绑 head_sha。
4. VERDICT=PASS → 合并 PR → branch 推进。
5. VERDICT=FAIL → 按 [WORKFLOW.md](./WORKFLOW.md) 第 4 节建 F-0NN，**不合并**，等修复后重跑。
6. 记录闭环耗时（从 impl 领卡到 merge 的 wall clock），评论在卡底。

## 不要做什么

- 不要在 verify FAIL 时强行合并（违反门禁）。
- 不要让 verify AI 读 impl AI 的过程评论（盲一半协议，见 [盲一半协议.md](../docs/盲一半协议.md)）。
- 不要跳过 VERDICT 绑 head_sha（防 commit 漂移）。

## 验收

详见 [V-008](./V-008.md)：★ 闭环验证。从 WAVE-2 抽卡走完整闭环，PR 合并后 VERDICT=PASS 且 branch 推进。

## 评论

领卡 @sandbox-trae-k2 @2026-07-30T09:38:00Z

_（领卡/完成时在此评论。本卡完成后在此评论"P4 闭环达成 @日期"，并在 WORKFLOW.md 顶部加"已停用"）_

- 领卡 @all-in-one-52-2rdcd-1619 @2026-07-30T02:05:41Z。role: impl（暂行期单 AI 切角色：impl W2-002 → 切 verify 跑 V-008）。依赖自检：C-004 done / C-005 done / C-007 done 全满足。从 WAVE-2 抽 verify.required=true 卡 **W2-002**（/health 端点，depends_on=[]，paths=src/api/health.py + tests/test_health.py，无 CODEOWNERS 占用）。impl 用 Python stdlib（无外部依赖，规避 loop upstream）。闭环计时起点 02:05:41Z。

