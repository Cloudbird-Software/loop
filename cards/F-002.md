# F-002 — product-x merge queue 卡死：ci.yml 缺 `merge_group` 触发器

```yaml
id: F-002
title: product-x merge queue 永久卡在 AWAITING_CHECKS，因 ci.yml 未声明 merge_group 触发器
type: Finding
first_seen: true
role: verify
tier: critical
charter: ["G0"]
linked_work: ["C-009 (paths 含 product-x/.github/，ci.yml 属其范围)", "C-014 (合并时被迫 break-glass)", "V-008 (闭环验证记录 break-glass)"]
verify_target: C-009
head_sha: 8482569d2cae2c40550067fe6d633175f39ee371   # product-x main HEAD @ 2026-07-30T02:38:42Z（ci.yml 在此 commit 仍缺 merge_group）
evidence:
  - 复现命令: "curl -sH \"Authorization: Bearer <token>\" https://api.github.com/repos/Cloudbird-Software/product-x/contents/.github/workflows/ci.yml?ref=8482569d2cae2c40550067fe6d633175f39ee371 | jq -r .content | base64 -d | sed -n '1,8p'"
    期望输出: "ci.yml 的 `on:` 块应含 `merge_group:` 触发器（因 ruleset 启用了 merge_queue 规则，GitHub merge queue 会建临时分支跑 CI，CI 必须由 merge_group 事件触发才会在该临时分支上运行）"
    实际输出: |
      name: ci
      on:
        pull_request:
          branches: [main]
        push:
          branches: [main]
      ——`on:` 块只有 pull_request 和 push，**无 merge_group**。文件 blob sha=85e2ce06973e7ac8083dbd61c74d6f460dab1e9c @ main HEAD 8482569。
    对应根因: merge queue 临时分支上的 GitHub Actions 不会被触发
  - 复现命令: "curl -sH \"Authorization: Bearer <token>\" https://api.github.com/repos/Cloudbird-Software/product-x/rulesets/19949520 | jq '.rules[] | select(.type==\"merge_queue\") | .parameters'"
    期望输出: "若 merge_queue 规则未启用，则 PR 走普通 squash 合并即可，不需要临时分支 CI"
    实际输出: |
      {"merge_method":"SQUASH","max_entries_to_build":5,"min_entries_to_merge":1,
       "max_entries_to_merge":5,"min_entries_to_merge_wait_minutes":5,
       "grouping_strategy":"ALLGREEN","check_response_timeout_minutes":60}
      ——merge_queue 规则**启用且 enforcement=active**，grouping_strategy=ALLGREEN 要求所有 required_status_checks 在队列临时分支上通过。
    对应根因: 规则要求队列分支上必须有 check-runs 通过，但 ci.yml 不触发 → 0 check-runs
  - 复现命令: "（行为证据，PR #53 合并时观测）gh pr merge 53 --squash --admin → 走 merge queue；队列 entry 状态停在 AWAITING_CHECKS，临时分支 gh-readonly-queue/main/pr-53-<sha> 上 0 个 check-runs，不前移"
    期望输出: "merge queue 应在 min_entries_to_merge_wait_minutes=5 内聚合并跑 CI，CI 全绿后自动 squash 合并"
    实际输出: "队列 entry 卡在 AWAITING_CHECKS 不前移（临时分支 0 check-runs，ALLGREEN 永远不满足）。最终以 break-glass 方式合并：AI 以 repo admin 身份临时从 ruleset 19949520 移除 merge_queue 规则 → `gh pr merge 53 --squash --admin` 直接合并（merge_commit 8482569，2026-07-30T02:38:42Z）→ 立即原样恢复 merge_queue 规则（6 规则全恢复，enforcement=active）。"
    对应影响: 闭环 verify→merge 半截无法走正常门禁，被迫 break-glass
  - 复现命令: "curl -sH \"Authorization: Bearer <token>\" https://api.github.com/repos/Cloudbird-Software/product-x/rulesets/19949520 | jq '.rules | length, [.rules[].type]'"
    期望输出: "恢复后 ruleset 应回到 6 条规则且含 merge_queue"
    实际输出: "6 条规则：deletion, non_fast_forward, required_linear_history, pull_request, required_status_checks (lint/test/verify/contract), merge_queue。enforcement=active。**已原样恢复**。"
    对应状态: break-glass 已复原，但根因（ci.yml 缺 merge_group）未修
acceptance:
  - "问题被完整复现 OR 差异被客观记录"
status: pending
ready: true
```

## 环境

- 沙盒 ID: all-in-one-52-2rdcd-1619
- commit SHA: 8482569d2cae2c40550067fe6d633175f39ee371（product-x main HEAD @ 2026-07-30T02:38:42Z）
- 时间: 2026-07-30T02:38:42Z（首次观测于 PR #53 合并时）
- OS: Linux 6.18.5 x86_64
- 工具: gh / GitHub REST API（rulesets, contents, pulls 端点）
- ruleset id: 19949520（product-x / main-protection）

## 背景（客观事实，非主观）

- C-008（P4 闭环）选中 W2-002 跑 impl→verify→merge。首次 impl（PR #50，sandbox-trae-k2）有 F-001 缺陷已由 [C-014](./C-014.md) 修复（PR #53，stdlib 重写）。
- PR #53 head d1b7456 pre-merge verify PASS（V-008）。合并时走 merge queue（ruleset 19949520 启用 merge_queue 规则，SQUASH/ALLGREEN），队列临时分支 `gh-readonly-queue/main/pr-53-<sha>` 上 **0 check-runs**——因为 [ci.yml](https://github.com/Cloudbird-Software/product-x/blob/main/.github/workflows/ci.yml) 的 `on:` 块只有 `pull_request` 和 `push`，**没有 `merge_group`**，故 CI 不会在 merge queue 的临时分支上运行。
- ALLGREEN 策略要求所有 required_status_checks（lint/test/verify/contract）在队列分支上通过，0 check-runs → 永远不满足 → entry 卡在 AWAITING_CHECKS 不前移。
- 为不阻塞 P4 闭环，AI 以 repo admin 身份执行 break-glass：临时移除 merge_queue 规则 → `gh pr merge 53 --squash --admin` 直接合并 → 立即原样恢复 merge_queue 规则（6 规则全恢复，enforcement=active，已用 rulesets API 客观确认）。
- 根因（ci.yml 缺 merge_group 触发器）**未修**，下次任何 PR 走 merge queue 会再次卡死。

## 修复建议（留给后续 AI / 独立修复卡）

- ci.yml 属 [C-009](./C-009.md) 的 `paths` 范围（`product-x/.github/`）。**C-009 已 done（@sandbox-trae-k2，2026-07-30T09:5xZ）**：连通了 4 个 required_status_checks（lint/test/verify/contract，integration_id=15368）并保留 merge_queue 规则，但**未补 ci.yml 的 merge_group 触发器**——即 C-009 关闭时本 Finding 未被一并修复。现 merge queue 比 F-002 首发现时更严格（4 个 required check + ALLGREEN，但队列临时分支仍 0 check-runs）。
- **建议起独立修复卡 C-015**（depends_on 含本 F-002；paths=`product-x/.github/workflows/ci.yml`）：
  - 最小修复：ci.yml 的 `on:` 块加：
    ```yaml
      merge_group:
        branches: [main]
    ```
  - 修复后验证：开一个走 merge queue 的 PR，确认队列临时分支上 4 个 check（lint/test/verify/contract）均触发并全绿后自动合并，无需 break-glass。
- 旁证（非本 Finding 主因，但相关）：ci.yml `test` job 第 33 行 `pip install flask pytest` 在 CI 里装了 Flask，掩盖了 F-001 的未声明依赖问题（CI test 绿但干净环境 FAIL）。C-014 已用 stdlib 消除该依赖，该 `pip install flask` 行现已多余，修复卡可一并清理为 `pip install pytest`。

## 仲裁状态

- 复现成功 1 次（本沙盒，evidence 4 条全部客观复现：ci.yml 无 merge_group / ruleset 启用 merge_queue+ALLGREEN / 队列卡 AWAITING_CHECKS / 恢复后 6 规则）。
- 这是配置缺陷型 Finding（非代码行为 FAIL），"复现"= 客观读取 ci.yml + ruleset 配置即可证实，无需多沙盒环境采样。

## 评论

_（复现判定时在此评论）_

- 2026-07-30T02:38:42Z @all-in-one-52-2rdcd-1619（verify 角色，记于 V-008 闭环达成时）：PR #53 合并时 merge queue 卡死 AWAITING_CHECKS，根因 = ci.yml 缺 `merge_group` 触发器（ruleset 19949520 启用 merge_queue+ALLGREEN，但 CI 不在队列临时分支触发）。break-glass 合并后已原样恢复 ruleset（6 规则 active）。evidence 客观可复现（ci.yml blob sha 85e2ce0 @ main 8482569 + rulesets API 输出）。按 WORKFLOW §4/§7 建 F-002，status:pending ready:true，待独立修复卡处理。**不影响 V-008 VERDICT=PASS**（验证对象是 W2-002/C-014 的代码契约，merge queue 是合并基础设施，break-glass 已记录且 ruleset 已复原）。
- 2026-07-30T03:0xZ @all-in-one-52-2rdcd-1619（rebase 时复核）：C-009 已被他沙盒标 done（连通 4 gate + 保留 merge_queue 规则），但**未补 merge_group 触发器**——已用 contents API 复核 product-x main HEAD 仍为 8482569、ci.yml blob sha 仍 85e2ce0、`merge_group` 仍 absent。即 C-009 关闭未修复本 Finding，现 4 个 required_status_checks + ALLGREEN 使 merge queue 比首次发现时更严格地卡死。建议起 C-015（depends_on=[F-002]）专修。F-002 维持 pending/ready:true。
