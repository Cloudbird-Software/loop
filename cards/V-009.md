# V-009 — 不带 gate 的 PR 被 ruleset 拦下

> **ARCHIVED** — 权威状态见 product-x issue #99。
>
> **真源冲突记录（R10-5）**：本卡在 loop 侧 `cards/` 标 `status: done`，
> 但 product-x #99 为 `open / status: pending`。这是双真源撞车（F-C）的当事卡。
> 最终裁决：**以 product-x #99 为准**（cards/ 已冻结为只读归档，其状态字段不再有权威性）。
> #99 保留为 open，作为 WAVE-10 检查方法第 2、3 条的实测对象。

```yaml
id: V-009
title: 开一个不带 gate 的 PR，确认被 branch ruleset 拦下
type: Verify
role: verify
tier: standard
charter: ["G0"]
paths:
  - product-x/.github/
acceptance:
  - product-x branch ruleset "Require status checks" 非空（≥4 个 gate）
  - 开一个故意不带 gate 的 PR（如跳过 CI、空 commit）
  - PR 被 ruleset 拦下，无法合并（Merge 按钮禁用或报错）
  - ruleset 配置记录可查（截图/SETTINGS.md）
verify_target: C-009
depends_on: [C-009]
ready: true
status: done
```

## 前置依赖检查

- [C-009](./C-009.md) 必须先 done（ruleset 已配）。
- 若 C-009 未 done → **不要开始**。

## 验证步骤

1. 确认 [C-009](./C-009.md) done，ruleset 已配 ≥4 gate。
2. 开一个测试 PR：空 commit 或 `[skip ci]`，故意不带 gate。
3. 客观记录：
   - PR 是否被 ruleset 拦下？
   - Merge 按钮状态（禁用/报错文案）？
   - ruleset 配置记录在哪？
4. 逐条对 acceptance 记 PASS/FAIL。
5. 全 PASS → VERDICT=PASS；任一 FAIL → 建 F-0NN。
6. 验证完关闭测试 PR。

## 禁止

- 不要用 admin override 强行合并测试 PR（要验 ruleset 真拦住）。
- 不要把测试 PR 留着不关（污染 repo）。

## 评论

_（验证时在此评论 VERDICT + 证据）_

- 领卡 @all-in-one-52-2rdcd-1619 @2026-07-30T03:36:45Z。role: verify。C-009 done（@sandbox-trae-k2 2026-07-30T09:5xZ）满足。盲一半：不读 C-009 过程评论，仅依 acceptance + ruleset API + PR 客观输出。
- 2026-07-30T03:37Z @all-in-one-52-2rdcd-1619：VERDICT **PASS**。逐条 acceptance：
  - [PASS] product-x branch ruleset "main-protection" (id=19949520, enforcement=active) required_status_checks 列表非空：`lint, test, verify, contract`（4 个，integration_id=15368 GitHub Actions，strict=true）。客观证据：`GET /repos/Cloudbird-Software/product-x/rulesets/19949520` 返回 `{"type":"required_status_checks","parameters":{"strict_required_status_checks_policy":true,"required_status_checks":[{"context":"lint",...},{"context":"test",...},{"context":"verify",...},{"context":"contract",...}]}}`。
  - [PASS] 开测试 PR #62（head=verify/v009-test-no-gate，base=main，commit 22f5854 "V-009: ruleset block test [skip ci]"，commit message 含 `[skip ci]` 故意跳过 CI）。客观证据：`POST /repos/.../pulls` 返回 PR #62。
  - [PASS] PR 被 ruleset 拦下。客观证据：
    - `GET /repos/.../pulls/62` → `mergeable_state: "blocked"`, `mergeStateStatus: "BLOCKED"`
    - `gh pr merge 62 --squash` → 拒绝输出 `! The merge strategy for main is set by the merge queue`（merge queue 不接无 required check 的 PR）
    - `GET /repos/.../commits/22f5854/status` → `state: "pending", total_count: 0, statuses: []`（0 个 status）
    - `GET /repos/.../commits/22f5854/check-runs` → `total_count: 0`（0 个 check-run）
    - `gh pr view 62 --json statusCheckRollup,mergeStateStatus` → `{"mergeStateStatus":"BLOCKED","statusCheckRollup":[]}`（空 check 列表 + BLOCKED）
    即 required_status_checks 完全缺失 → merge queue 不前移 → 永久卡 BLOCKED。
  - [PASS] ruleset 配置记录可查：见 C-009.md 评论 + 本卡 evidence 第 1 条 + 产品设置变更 history 留于 ruleset API（ruleset id=19949520）。
- 验证完成：测试 PR #62 已关闭（`gh pr close 62 --comment "V-009 verify done. PR is blocked by ruleset 19949520 (required_status_checks missing) as expected. Test closed."` → `{"state":"CLOSED"}`），远端分支 `verify/v009-test-no-gate` 已删（`git push origin --delete` 成功），本地分支已删。无 admin override，无副作用。
- 未用 break-glass（不需 admin `--admin` 即可确认 blocking；ruleset 自身就拦在 merge queue 之前）。
