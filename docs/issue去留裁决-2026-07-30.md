# 既有 issue 去留裁决 — 2026-07-30

> 用户授予最终裁决权后，对两仓当前全部 **59 个 open issue**（loop 11 + product-x 48）
> 逐条给出去留意见。
>
> **重要**：我只有只读的 GitHub 查询能力，**无法关闭或创建 issue**。
> 下面每一节末尾附了可直接粘贴执行的 `gh` 批量命令，请你或一个有写权限的 agent 执行。
> 关闭前请扫一眼理由——我可能判错，你有否决权。

## 裁决原则

1. **已完成的留证不留单**：`status-done` 的卡，其价值在 evidence 与 git 历史，不在 open 列表。
2. **合成/测试产物一律关**：canary 合成工单、越权测试卡，它们的使命在被检测到的那一刻就完成了。
3. **重复单留旧关新**：保留最早的一张（有完整历史），关掉后来重复物化的。
4. **被新波次吸收的关闭并回指**：关闭时必须在评论里写清"被 WAVE-1x / R1x-y 吸收"，
   不留孤儿。
5. **真实且未被吸收的留着**：宁可留噪声，不可丢信号。
6. **Gripe Box 永不关**。

---

## 一、loop 仓（11 个 open）

| # | 标题 | 裁决 | 理由 |
|---|---|---|---|
| 3 | Incident: 26h survival check failed @07-29T10:15 | **关** | 与 #48 同指纹（26h 存活检查失败）。同指纹只留最新一张作为证据 |
| 4 | Incident: canary chain broken @07-29T10:16 | **留** | "canary chain broken" 指纹的唯一存活代表，留作证据 |
| 5 | Incident: 26h survival check failed @07-29T10:19 | **关** | 同 #3 |
| 6 | Incident: canary chain broken @07-29T10:19 | **关** | 与 #4 同指纹，#4 更早、历史更完整 |
| 47 | Incident: 26h survival check failed @07-30T09:27 | **关** | 同指纹，#48 更新 |
| 48 | Incident: 26h survival check failed @07-30T09:29 | **留** | 同指纹最新一张，留作证据；根因修复由 **R10-6** 承接（Incident 按指纹去重） |
| 49 | Add tests for loopd.do_save and autosave | **关（被吸收）** | 被 **R14-4**（零覆盖模块补测试）完整覆盖，且 R14-4 明确要求覆盖 loopd 承重路径 |
| 50 | Add GitHub Actions workflow to run pytest on PRs | **关（已完成）** | 本 PR 已交付 `.github/workflows/pr-ci.yml`，pytest 现已在 PR 上运行。剩下的"设为 required"是人类动作，见 HUMAN-TODO C1 |
| 51 | Fix NameError in loopd.do_save: unquoted Cloudbird-Software | **关（已修复）** | **本 PR 已修**。三处 `E.get("LOOP_ORG", Cloudbird-Software)` 缺引号，语法合法（被解析为减法）故 py_compile 放行，运行时必炸 NameError。已补 `tests/test_no_undefined_names.py` 作确定性回归检查器，并含负向自证用例 |
| 52 | Breaking: Remove remote/agent command bypass (relay/filemode/run) | **留** | 已核实 `loopd/loopd.py` 中 `RELAY` 与 `filemode_thread` 确实存在。这是真实的远程命令通道，属安全面决策，我不替你拍板是否移除。建议在 WAVE-11 期间作为独立安全卡处理 |
| 53 | Impl: remove relay/filemode/run code | **留** | 同 #52，是其实现单，与 #52 成对保留 |

**loop 侧批量命令**：

```bash
R=Cloudbird-Software/loop
for n in 3 5 6 47; do
  gh issue close $n -R $R -c "按 docs/issue去留裁决-2026-07-30.md：同指纹 Incident 只保留最新一张作为证据（26h 存活留 #48，canary chain 留 #4）。根因修复由 R10-6（Incident 按指纹去重）承接。"
done
gh issue close 49 -R $R -c "被 WAVE-14 的 R14-4（零覆盖模块补测试，含 loopd 承重路径）完整吸收。见 waves/WAVE-14.md。"
gh issue close 50 -R $R -c "已完成：本仓已新增 .github/workflows/pr-ci.yml，pytest 现于每个 PR 上运行。剩余的『设为 required check』是人类动作（CHARTER N5 禁止 AI 改 ruleset），见 HUMAN-TODO.md C1。"
gh issue close 51 -R $R -c "已修复：loopd.py 三处 f-string 中 Cloudbird-Software 缺引号被解析为减法表达式，运行时 NameError。已加 tests/test_no_undefined_names.py 作确定性回归检查器（含负向自证：该检查器能抓住原写法）。"
```

---

## 二、product-x 仓（48 个 open）

### 2.1 永久保留

| # | 标题 | 裁决 |
|---|---|---|
| 1 | Gripe Box | **永久留** — 它是人类唯一的低摩擦反馈入口，关掉等于关掉耳朵 |

### 2.2 重复物化，关新留旧

`w1-*` 五张卡被物化了两次。后一批标题带 `— ?`（`objective` 字段缺失导致），属畸形副本。

| 保留 | 关闭 |
|---|---|
| #18 w1-license | #45 |
| #19 w1-gitignore | #46 |
| #20 w1-readme-badge | #47 |
| #21 w1-editorconfig | #48 |
| #22 w1-agents | #49 |

> 附带发现：畸形副本的存在说明**物化器的幂等检查失效过**。
> `check_already_materialized()` 靠 `wave` 字段查重，而 `w1-*` 这批卡的 `wave` 可能为空。
> 已在 **R12-3** 的验收中要求"既有四项校验与幂等逻辑不得被削弱，回归测试必须覆盖"，
> 请领 R12-3 的 agent 顺带把空 wave 的幂等路径补上。

### 2.3 合成 / 测试产物，一律关

| # | 标题 | 理由 |
|---|---|---|
| 54 / 56 / 58 / 105 | canary: synthetic ticket | canary 合成工单，被检测到即完成使命。堆积本身是 **R10-6** 要治的噪声 |
| 109 | [Card] T6-OOO — 越权测试卡 (paths=tmp/phantom/**) | 越权阀门的测试卡，验证完毕即应关闭 |
| 68 | F-003 repro downstream (was blocked by ) | 标题里 `was blocked by ` 后面是空的——依赖注入模板漏参。F-003（tick.py 大小写比较）本身已修复（当前 `st.lower() != "closed"`）。此单无内容且无下游 |

### 2.4 已完成的 C 卡，关闭留证

`status-done` 的 C 卡：**#75 #76 #77 #78 #79 #80 #81 #82 #83 #84 #85 #88**（C-001~C-011、C-014）。

其中 **#84（C-010 起草 CHARTER.md）**特别说明：本 PR 已由 AI 代拟
`templates/product-x/CHARTER.md` 全文，落地动作在 **R13-3**，人类签署在 **HUMAN-TODO A1**。
关闭时请把这三个指针写进评论。

### 2.5 未完成的 C 卡，保留

| # | 标题 | 裁决 |
|---|---|---|
| 86 | C-012 验 canary | **留** — 两仓状态一致（ready:true / pending / open），是真实待办 |
| 87 | C-013 扩到 2-3 沙盒验并发 | **留** — CAS + 路径租约 + reaper 的并发验证从未真跑过，且 **R14-6** 的 7 天连续运行依赖它先跑通 |
| 107 | C-T6 paths-lease + verdict-binding gate 接进 ci.yml + ruleset | **关（被吸收）** — 被 **R11-1**（八道门禁全部接线并逐个证明会红）完整覆盖，且 R11-1 的要求更强（要求每道门禁都在真实 PR 上红过） |

### 2.6 F-002 与 C-015：**需先取证再裁决**

| # | 标题 | 裁决 |
|---|---|---|
| 89 | C-015 修复 F-002：ci.yml 加 merge_group | **暂留** |
| 90 | F-002 merge queue 永久卡在 AWAITING_CHECKS | **暂留** |

**为什么不直接关**：#89 标着 `status-done`，按 2.4 的原则本该关掉。但我**无法读取 product-x 的
`ci.yml`**（无跨仓读取能力），无法确认 `on:` 里真的有了 `merge_group`。
而这次审查的最大教训恰恰是"不要采信没被验证的状态字段"——loop 侧 V-009 标 `done`、
product-x #99 却是 `open/pending`，双真源撞车就是这么来的。

**取证命令**（一行，谁执行都行）：

```bash
gh api repos/Cloudbird-Software/product-x/contents/.github/workflows/ci.yml \
  --jq '.content' | base64 -d | grep -n "merge_group" || echo "MISSING: merge_group"
```

- 若命中 → 关闭 #89 与 #90，并在 **R10-2** 的验收里把 merge_group 那条改为"确认已存在"。
- 若 MISSING → 两单都留着，由 **R10-2** 一并修复（R10-2 已把它列为验收项）。

### 2.7 V 卡（#91–#103）：多数被新波次的验收方法吸收

| # | 卡 | 裁决 | 去向 |
|---|---|---|---|
| 91 | V-001 干净沙盒端到端 | **留** | 是 **R14-6**（7 天无人值守）的前置烟测，价值独立 |
| 92 | V-002 auditor 产 Finding | **关** | 被 **R14-1**（lens 产出真流入工单）吸收，且 R14-1 要求更强（finding_id 必须是真 issue 号） |
| 93 | V-003 planner 产 Wave PR | **关** | 被本 PR 恢复的 loop 物化器 + **R12-3** 角色阀门吸收 |
| 94 | V-004 verify 产 VERDICT | **关** | 被 **R11-2**（gate/heterogeneity 校验 verdict 的 verifier 与 impl 异构）吸收，要求更强 |
| 95 | V-005 materializer 物化 WAVE-2 | **关** | 本 PR 已本地跑通物化器全部校验（33 张卡全过）；线上验证由 WAVE-10..14 的实际物化天然覆盖 |
| 96 | V-006 tick 回收僵尸卡 + 依赖放行 | **留** | 承重的自愈路径，未被任何新卡覆盖 |
| 97 | V-007 verify.sh 非退化态 | **关** | 被 **R10-3**（未执行等价于失败）与 **R11-1** 吸收，且新要求直接禁掉了退化态本身 |
| 98 | V-008 完整闭环（★P4 终点） | **关** | `status-done`，按 2.4 原则关闭 |
| 99 | V-009 无 gate 的 PR 应被拦下 | **留** | **这是双真源撞车的当事卡**（loop 侧标 done，此处 open/pending）。留着，并由 **R10-5** 在归档 `cards/` 时显式记录该冲突与最终裁决。它同时是 WAVE-10 检查方法第 2、3 条的实测对象 |
| 100 | V-010 按 G/N/Q 过滤不再用 G0 占位 | **留** | 本 PR 已给 loop 的 `CHARTER.md` 补了机器可读索引（G0-G5 / N3-N15 / Q0-Q5），product-x 侧待 **R13-3** 落地 CHARTER 后此卡才能验；保留作为验收锚点 |
| 101 | V-011 scribe 推第二 remote | **关** | 被 **R11-4**（scribe token 移出 URL）吸收，该卡会连带验证推送链路 |
| 102 | V-012 canary 合成工单被检测告警 | **留** | 与 #86（C-012）成对。注意：此单的 label 与自身 body 不同步，是 product-x 内部状态不一致，**不是**两源撞车——专家称"V-012 同理"是过度泛化，实测两边 body 一致 |
| 103 | V-013 两沙盒并发 CAS/租约/reaper | **留** | 与 #87 成对，是 **R14-6** 的前置 |

### 2.7b 其他

| # | 标题 | 裁决 |
|---|---|---|
| 35 | [Card] e2-handoff | **关** — 单次交接卡，其内容已沉淀进 `docs/archive/E2-IMPL2.md` |

### product-x 侧批量命令

```bash
R=Cloudbird-Software/product-x

# 2.2 重复物化的畸形副本
for n in 45 46 47 48 49; do
  gh issue close $n -R $R -c "重复物化的畸形副本（objective 缺失导致标题为 '— ?'），保留最早一批 #18-#22。见 docs/issue去留裁决-2026-07-30.md 2.2。物化器幂等缺口由 R12-3 顺带修复。"
done

# 2.3 合成 / 测试产物
for n in 54 56 58 105; do
  gh issue close $n -R $R -c "canary 合成工单，被检测到即完成使命。堆积噪声的根治由 R10-6（Incident/合成单按指纹去重）承接。"
done
gh issue close 109 -R $R -c "越权阀门测试卡（paths=tmp/phantom/**），验证完毕。角色阀门的常态化回归由 R12-3 承接。"
gh issue close 68  -R $R -c "空壳单（标题依赖注入模板漏参，正文无内容）。F-003 本身已修复：tick.py 现为 st.lower() != 'closed'。"

# 2.4 已完成的 C 卡
for n in 75 76 77 78 79 80 81 82 83 85 88; do
  gh issue close $n -R $R -c "status-done，按 docs/issue去留裁决-2026-07-30.md 2.4 关闭留证（价值在 evidence 与 git 历史，不在 open 列表）。"
done
gh issue close 84 -R $R -c "CHARTER 已由 AI 代拟全文，见 loop 仓 templates/product-x/CHARTER.md；落地动作见 R13-3，人类签署见 loop 仓 HUMAN-TODO.md A1。"

# 2.5 被新波次吸收
gh issue close 107 -R $R -c "被 WAVE-11 的 R11-1 完整吸收（八道门禁全部接线，且每道必须在真实 PR 上红过一次）。见 loop 仓 waves/WAVE-11.md。"

# 2.7 被新波次吸收的 V 卡
gh issue close 92 -R $R -c "被 R14-1 吸收（lens 产出真流入工单，finding_id 必须是真实 issue 号）。"
gh issue close 93 -R $R -c "被 loop 侧恢复的物化器 + R12-3 角色阀门吸收。"
gh issue close 94 -R $R -c "被 R11-2（gate/heterogeneity：verdict 的 verifier 必须与 impl 异构）吸收，要求强于原卡。"
gh issue close 95 -R $R -c "物化器四项校验 + paths 两两不交叉已在 loop 本地对 33 张新卡跑通；线上验证由 WAVE-10..14 实际物化天然覆盖。"
gh issue close 97 -R $R -c "被 R10-3（任何 gate 未执行等价于失败）与 R11-1 吸收——新要求直接禁掉了退化态本身。"
gh issue close 98 -R $R -c "status-done，按 2.4 关闭留证。"
gh issue close 101 -R $R -c "被 R11-4（scribe token 移出 remote URL）吸收，该卡会连带验证推送链路。"

# 2.7b
gh issue close 35 -R $R -c "单次交接卡，内容已沉淀进 loop 仓 docs/archive/E2-IMPL2.md。"
```

---

## 三、执行后的结果

| 仓库 | 当前 open | 建议关闭 | 剩余 | 新增（物化后） |
|---|---|---|---|---|
| loop | 11 | 7 | 4 | +39（5 个 Wave 父单 + 33 张卡 + milestone 不计） |
| product-x | 48 | 30 | 18 | 0（本轮新卡全部在 loop 建单，跨仓卡以 `repo` 字段标注） |

**剩余的 loop 4 张**：#4、#48（两条不同指纹的 Incident 证据）、#52、#53（relay/filemode 安全决策）。

**剩余的 product-x 18 张**：#1（Gripe Box）、#18-22（w1-* 原始批）、#86、#87、#89、#90、
#91、#96、#99、#100、#102、#103。

---

## 四、我可能判错的地方（请你复核）

老实说三处我信心不足：

1. **#52 / #53（移除 relay/filemode/run）** —— 我核实了代码确实存在，但"是否该移除"是安全面
   的产品决策。我倾向移除（远程命令通道是最典型的横向移动入口），但没有替你拍板。
2. **#89 / #90（F-002 merge_group）** —— 我读不到 product-x 的 `ci.yml`，所以拒绝仅凭
   `status-done` 标签就关单。见 2.6 的取证命令。
3. **#18-22（w1-* 原始批）** —— 我保留了它们，但没看内容，不确定是否也已完成。
   若已完成请一并关闭。

这三处的共同点是：**我缺少可复现的证据**。按本轮定下的规矩（ADR-004/005），
证据不足时应当标注不确定并交由复现，而不是硬给结论。我照做了。
